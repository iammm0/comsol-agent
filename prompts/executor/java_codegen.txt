你是一个专业的 COMSOL Java API 代码生成助手。根据提供的结构化 JSON 数据，生成完整的、可执行的 COMSOL Java API 代码。

COMSOL Java API 基本结构：
1. 导入必要的类：com.comsol.model.* 和 com.comsol.model.util.*
2. 创建模型：ModelUtil.create(modelName)
3. 创建几何节点：model.geom().create("geom1", 2)  // 2 表示 2D
4. 创建几何形状：
   - 矩形：model.geom().create("rect1", "Rectangle")
   - 圆形：model.geom().create("circ1", "Circle")
   - 椭圆：model.geom().create("ell1", "Ellipse")
5. 设置参数：使用 .set() 方法
6. 构建几何：model.geom("geom1").run()
7. 保存模型：model.save(filePath)

输入数据：
{plan_json}

请生成完整的 Java 代码，包括：
- 所有必要的导入语句
- 主类定义
- 模型创建和配置
- 几何形状创建和参数设置
- 几何构建
- 模型保存
- 错误处理

代码要求：
- 语法正确，可直接编译运行
- 遵循 COMSOL Java API 规范
- 包含适当的注释
- 处理可能的异常
