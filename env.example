# COMSOL Agent 环境变量示例
# 复制此文件为 .env 并填写实际值。不要将 .env 提交到仓库。

# ----- LLM 后端 -----
# 可选: dashscope | openai | openai-compatible | ollama
# 不设置时默认 dashscope
LLM_BACKEND=dashscope

# ----- Dashscope (通义千问) -----
# 当 LLM_BACKEND=dashscope 时必填。也可通过 keyring 存储，不写在此文件。
DASHSCOPE_API_KEY=

# ----- OpenAI 官方 API -----
# 当 LLM_BACKEND=openai 时使用
OPENAI_API_KEY=
# 可选，不设置则使用官方 API 地址
# OPENAI_BASE_URL=
OPENAI_MODEL=gpt-3.5-turbo

# ----- OpenAI 兼容 API（第三方） -----
# 当 LLM_BACKEND=openai-compatible 时必填
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_MODEL=gpt-3.5-turbo

# ----- Ollama（本地/远程） -----
# 当 LLM_BACKEND=ollama 时使用；不需要 API Key
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# ----- COMSOL -----
# COMSOL JAR 路径，必填
COMSOL_JAR_PATH=
# Java 安装目录，可选，不设置时使用系统环境变量 JAVA_HOME
# JAVA_HOME=

# ----- 输出与日志 -----
# 模型输出目录，不设置时默认为安装目录下的 models
# MODEL_OUTPUT_DIR=
# 日志级别: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
